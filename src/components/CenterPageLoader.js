import Loading from "./Loading";

const CenterPageLoader = () => {
  return (
    <div className="complete-center" style={{ height: "60vh", width: "100vw" }}>
      <Loading />
    </div>
  );
};

export default CenterPageLoader;
